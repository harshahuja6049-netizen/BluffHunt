# Leave empty when the backend serves the built frontend (same origin).
# Local Vite already talks to this machine's port 5000.
# Split hosting example:
# VITE_SOCKET_URL=https://your-api.example.com
VITE_SOCKET_URL=
